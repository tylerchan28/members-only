extends layout 

block content 
  div.member-container
    h1 Become an Admin

    p Enter the secret code to become an admin.  
    
    form.member-form(method="POST", action="")
      input#code.form-control(type="text", name="code", required="true")
      button.member-btn(type="submit") Submit

    if errors 
      each error in errors 
        li= error.msg